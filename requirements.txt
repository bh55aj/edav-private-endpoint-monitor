# EDAV Private Endpoint Monitor v4.0.0 - Python Dependencies
# Install with: pip install -r requirements.txt

# Azure CLI must be installed separately:
# https://aka.ms/installazurecliwindows

# Data processing and Excel report generation
pandas>=2.0.0
openpyxl>=3.1.0

# HTML/JSON reports use Python stdlib only (no extra packages needed)
# Email notifications use Python stdlib smtplib (no extra packages needed)
# Terraform checks use subprocess (no extra packages needed)
