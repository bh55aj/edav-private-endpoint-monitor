# EDAV Resource Monitor Cleanup Platform - v5.0.0
# Python dependencies

# Core data processing
pandas>=2.0.0
openpyxl>=3.1.0

# YAML config support
pyyaml>=6.0.0

# HTML report generation
jinja2>=3.1.0

# Colored console output
colorama>=0.4.6

# Excel formatting (for color-coded reports)
xlsxwriter>=3.1.0

# Optional: for enhanced date parsing in input files
python-dateutil>=2.8.2

# Optional: for email notifications
# smtplib is built-in; no extra package needed

# Optional: for Azure SDK (future enhancement - currently uses az CLI)
# azure-mgmt-resource>=23.0.0
# azure-mgmt-compute>=30.0.0
# azure-mgmt-network>=25.0.0
# azure-mgmt-storage>=21.0.0
# azure-mgmt-keyvault>=10.0.0
# azure-identity>=1.15.0
